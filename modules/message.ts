export default function hello() { //this function will be imported by default, if we won't specify what do we want to export explicitly in {}
    console.log("Hello Typescript");
}

let a = function (){};